import React from "react"
import Layout from "../components/layout"

export default () => (
  <Layout>
    <h3>Hello World!</h3>
    <p>
      This is the intro page. It will need to have some cool animations to grab
      the user's attention
    </p>
  </Layout>
)
